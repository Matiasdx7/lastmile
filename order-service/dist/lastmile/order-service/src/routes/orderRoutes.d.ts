import { Router } from 'express';
import { Pool } from 'pg';
import { RedisClientType } from 'redis';
export declare const createOrderRouter: (pool: Pool, redisClient?: RedisClientType | null) => Router;
//# sourceMappingURL=orderRoutes.d.ts.map